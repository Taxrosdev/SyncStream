# SyncStream

SyncStream is a simple utility to syncronize the local state of a directory with a remote, designed as an internal for Immutable Distro/Package Manager use cases.

## Glossary

`Repository` -> The remote location for storing chunks

`Store` -> The local location for storing streams

`Chunk` -> A 4MB Section of a file

`Stream` -> The files chunks and it's metadata
